{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Contacter un vendeur</h1>

    <div class="container mt-5">
        {{ form_start(form) }}
            {{ form_widget(form) }}
        <div class="row mt-5 justify-content-center">
            <button class="btn btn-primary" type="submit">Envoyer</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}